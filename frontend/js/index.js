const { default: PagesManifestPlugin } = require("next/dist/build/webpack/plugins/pages-manifest-plugin");

/**
 * Fonction main qui va contenir le code de base pour éviter
 * les variables au niveau du script de base
 */
main()

function main() {
    //Recupération des produits
    const products = getProducts();
    //Fonctions qui affiche tous les articles
    displayProducts(products);
}

function getProducts() {
    
}

function displayProducts() {

}